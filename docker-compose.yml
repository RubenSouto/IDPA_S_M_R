version: "3"
services:
  frontend:
    container_name: idpa-frontend
    build:
      context: ./frontend
      dockerfile: ./docker/Dockerfile
    volumes:
      - ./frontend/lernApp:/lernApp
      
    ports:
      - "4200:4200"

  # backend:
  #   container_name: idpa-backend
  #   build:
  #     context: ./backend
  #     dockerfile: ./docker/Dockerfile
  #   command: sh -c '/docker-entrypoint.sh'
  #   ports:
  #       - "8020:80"
  #  volumes:
  #      - ./backend/app:/var/www/html